import utils.loggerInitializer  # initializing logger don't delete
import pause
import logging
from settings import UPDATE_INTERVAL, load_yaml


log = logging.getLogger()

if __name__ == '__main__':
    log.info("HW")
